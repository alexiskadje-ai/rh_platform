import PlaceholderPage from "@/components/layout/placeholder-page";

export default function ContactPage() {
  return (
    <PlaceholderPage
      title="Contact"
      body="Le formulaire de contact public sera branché sur les notifications (Phase 8)."
    />
  );
}
